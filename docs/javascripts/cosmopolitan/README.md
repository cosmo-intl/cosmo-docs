# Vendored cosmopolitan-js

These files are the compiled ESM build copied verbatim from `cosmopolitan-js`
(v1.1.0) `dist/*.js`. They power the live [Playground](../../playground.md).

To update: rebuild the JS port (`npm run build`) and re-copy:

```bash
cp ../../../cosmopolitan-js/dist/*.js .
```
